# Environment Variables for ProxLLM
# Copy this file to .env.local and fill in your API keys (optional - you can also use the Settings UI)

# Database URL for PostgreSQL (required for production)
DATABASE_URL="postgresql://username:password@localhost:5432/proxllm"

# Clerk Authentication (replace NextAuth in production)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your-clerk-publishable-key-here
CLERK_SECRET_KEY=sk_test_your-clerk-secret-key-here
CLERK_WEBHOOK_SECRET=whsec_your-webhook-secret-here

# NextAuth configuration (kept for migration period)
NEXTAUTH_URL="https://your-app.vercel.app"
NEXTAUTH_SECRET="your-nextauth-secret-key-here"

# Encryption key for storing API keys (exactly 32 characters - REQUIRED)
ENCRYPTION_KEY=change-this-32-character-key-now!

# OpenAI API Key (optional fallback)
OPENAI_API_KEY=sk-your-openai-key-here

# Anthropic API Key (optional fallback)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Google AI API Key (optional fallback)
GOOGLE_API_KEY=your-google-ai-key-here

# Note: The application will first try to use API keys added through the Settings UI,
# and only fall back to these environment variables if no keys are found in Settings.